{
  "installCommand": "npm install --force",
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    },
    {
      "rewrites": [{ "source": "/(.*)", "destination": "/" }]
    }
  ]
}

